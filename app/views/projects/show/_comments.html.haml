.comments
  %h2 Activity
  .well.well-sm
    - if current_project.posts.count==0
      .lead.text-center.well-lg
        %i.fa.fa-list
        Be the first to post comments on this project
    - current_project.posts.each do |post|
      - if post.persisted?
        = render 'projects/show/comment', :post => post
    = render 'components/post/add_post', :project => current_project, :post => @post
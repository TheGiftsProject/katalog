.row
  .col-xs-12.col-sm-3.pull-right
    = render 'projects/index/quick_create'

  .col-xs-12.col-sm-9
    .panel
      - if @projects.empty?
        = render 'projects/index/no_projects'
      - else
        - @projects.each do |project|
          = render 'projects/index/project_row', :project => project

-#- if @viewing_projects_of
-#  .project-by{:style => 'margin-bottom: 5px; margin-left: 2px;'}
-#    %i.icon-download-alt
-#    %h5{:style => 'display:inline;'}= t('pages.index.viewing_projects_of', :user_name => @viewing_projects_of.name)
